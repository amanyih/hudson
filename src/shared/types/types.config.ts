export enum MODE {
  WORDS = 'WORDS',
  TIME = 'TIME',
}

export enum SUBMODE {
  TWENTY_FIVE = 25,
  FIFTY = 50,
  HUNDRED = 100,

  FIFTEEN_SECONDS = 15,
  THIRTY_SECONDS = 30,
  SIXTY_SECONDS = 60,
  ONE_TWENTY = 120,
}

export enum BADGETYPE {
  WPM = 'WPM',
  ACCURACY = 'ACCURACY',
  TESTS = 'TESTS',
  TIME = 'TIME',
}

export enum THEME {
  SYSTEM = 'SYSTEM',
  LIGHT = 'LIGHT',
  DARK = 'DARK',
}
